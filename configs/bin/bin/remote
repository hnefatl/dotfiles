{%- if WORK -%}
#!/usr/bin/env bash

#if ! gcertstatus --check_ssh --quiet ; then
#    gcert --corpssh --prodssh || exit 1
#fi
#ssh kcollister.zrh.corp.google.com

# sudo apt install roadwarrior

if [[ -z $1 ]] ; then
    echo "Usage: $0 <session ID> [args...]"
    exit 1
fi

workspace_name="$1"
shift

rw kcollister-cloud.c.googlers.com -S --shpool_session="$workspace_name" --enable_x_forwarding --check_remaining --reconnect_loop

{%- endif %}
